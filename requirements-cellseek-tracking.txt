# CellSeek tracking: cellsam (segmentation + propagation API) and CUTIE backend.
#   cellseek/
#     cellsam/                <-- pip install -e from here
#     cutie/                  <-- pip install -e from here (MaskTracker backend)
#     cellseek_benchmark/
#
# Run from cellseek_benchmark/:
#   pip install -r requirements.txt
#   pip install -r requirements-cellseek-tracking.txt

-e ../cellsam
-e ../cutie
