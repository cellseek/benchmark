# Omnipose segmentation on CTC BF-C2DL-HSC — GT from SEG/man_seg only (sparse, ~57 frames).
# Slurm: sbatch sbatch.sh
# CLI:
#   python -m cellseek_benchmark.test --config-dir configs \
#     --benchmark-config experiments/omnipose_ctc_bf_c2dl_hsc_segmentation.yaml

experiment_id: omnipose_ctc_bf_c2dl_hsc_segmentation

output_parent: experiments

dataset: ctc_bf_c2dl_hsc
model: omnipose
tasks: segmentation

datasets_config: datasets.yaml
models_config: models.yaml
metrics_config: metrics.yaml
